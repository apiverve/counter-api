name: apiverve_counter
description: Counter is a simple tool for incrementing, decrementing, and resetting a cloud counter. It returns the current value of the counter.
version: 1.1.14
homepage: https://apiverve.com/marketplace/counter?utm_source=dart&utm_medium=homepage
repository: https://github.com/apiverve/counter-api/tree/main/dart

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.1.0
  http_parser: ^4.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
